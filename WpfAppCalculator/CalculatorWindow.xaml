<Window x:Class="WpfAppCalculator.CalculatorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfAppCalculator"
        mc:Ignorable="d"
        Title="MainWindow" Height="600" Width="350" Icon="/calculator.bmp">
    <Window.Resources>
        <LinearGradientBrush x:Key="buttonBackground" StartPoint="0.5,0.25">
            <GradientStop Color="White" Offset="0"/>
            <GradientStop Color="Gray" Offset="1"/>
        </LinearGradientBrush>
        <RadialGradientBrush x:Key="screenBackground" GradientOrigin="0.5,1" RadiusX="1" RadiusY="0.7">
            <GradientStop Color="White" Offset="0"/>
            <GradientStop Color="LightGray" Offset="0.9"/>
            <GradientStop Color="White" Offset="1.5"/>
        </RadialGradientBrush>
        <LinearGradientBrush x:Key="borderBackground" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="White" Offset="0"/>
            <GradientStop Color="Gray" Offset="1"/>
        </LinearGradientBrush>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="2"/>
            <Setter Property="FontSize" Value="28"/>
            <Setter Property="FontFamily" Value="Unispace"/>
            <Setter Property="Background" Value="{StaticResource buttonBackground}"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect/>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border 
                            Background="{TemplateBinding Background}" 
                            CornerRadius="15"
                            BorderThickness="1"
                            BorderBrush="Gray"
                            SnapsToDevicePixels="True">
                            <ContentPresenter 
                                HorizontalAlignment="Center" 
                                VerticalAlignment="Center"
                                RecognizesAccessKey="True"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="Border" x:Key="borderStyle">
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Background" Value="{StaticResource borderBackground}"/>
            <Setter Property="CornerRadius" Value="25"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="BorderBrush" Value="Gray"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect/>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="Border" x:Key="innerBorderStyle">
            <Setter Property="Padding" Value="15 10"/>
            <Setter Property="Margin" Value="0 0 0 10"/>
            <Setter Property="Background" Value="{StaticResource screenBackground}"/>
            <Setter Property="CornerRadius" Value="25"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="Gray"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect/>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="TextBlock" x:Key="curNumTextBlockStyle">
            <Setter Property="FontSize" Value="45"/>
            <Setter Property="FontFamily" Value="Reem Kufi Bold"/>
            <Setter Property="TextAlignment" Value="Right"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Padding" Value="5"/>
        </Style>
        <Style TargetType="TextBlock" x:Key="prevOperationsTextBlockStyle">
            <Setter Property="FontSize" Value="32"/>
            <Setter Property="FontFamily" Value="Reem Kufi"/>
            <Setter Property="TextAlignment" Value="Right"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Padding" Value="5"/>
        </Style>
    </Window.Resources>

    <Border Style="{StaticResource borderStyle}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="6*"/>
                <RowDefinition Height="14*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Style="{StaticResource innerBorderStyle}">
                <Grid Grid.Row="0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="2*"/>
                    </Grid.RowDefinitions>
                    <TextBlock x:Name="PreviousOperations" Grid.Row="0" Style="{StaticResource prevOperationsTextBlockStyle}"/>
                    <TextBlock x:Name="CurrentNumber" Grid.Row="1" Style="{StaticResource curNumTextBlockStyle}"/>
                </Grid>
            </Border>
            <Grid Grid.Row="1">
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>

                <Button Content="C" Grid.Row="0" Grid.Column="0" Click="C_Click"/>
                <Button Content="CE" Grid.Row="0" Grid.Column="1" Click="CE_Click"/>
                <Button Content="&lt;" Grid.Row="0" Grid.Column="2" Click="Backspace_Click"/>
                <Button Content="/" Grid.Row="0" Grid.Column="3" Click="Operator_Click"/>

                <Button Content="7" Grid.Row="1" Grid.Column="0" Click="Number_Click"/>
                <Button Content="8" Grid.Row="1" Grid.Column="1" Click="Number_Click"/>
                <Button Content="9" Grid.Row="1" Grid.Column="2" Click="Number_Click"/>
                <Button Content="*" Grid.Row="1" Grid.Column="3" Click="Operator_Click"/>

                <Button Content="4" Grid.Row="2" Grid.Column="0" Click="Number_Click"/>
                <Button Content="5" Grid.Row="2" Grid.Column="1" Click="Number_Click"/>
                <Button Content="6" Grid.Row="2" Grid.Column="2" Click="Number_Click"/>
                <Button Content="-" Grid.Row="2" Grid.Column="3" Click="Operator_Click"/>

                <Button Content="1" Grid.Row="3" Grid.Column="0" Click="Number_Click"/>
                <Button Content="2" Grid.Row="3" Grid.Column="1" Click="Number_Click"/>
                <Button Content="3" Grid.Row="3" Grid.Column="2" Click="Number_Click"/>
                <Button Content="+" Grid.Row="3" Grid.Column="3" Click="Operator_Click"/>

                <Button Content="." Grid.Row="4" Grid.Column="0" Click="Number_Click"/>
                <Button Content="0" Grid.Row="4" Grid.Column="1" Click="Number_Click"/>
                <Button Content="=" Grid.Row="4" Grid.Column="2" Grid.ColumnSpan="2" Click="Equals_Click"/>
            </Grid>
        </Grid>
    </Border>
</Window>
